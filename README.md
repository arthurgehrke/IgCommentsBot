I used puppeteer in order to get instagram page, so I can manipulate DOM elements.
I also used just pure javascript, because instagrams page dont use jQuery, and i didnt want to inject jQuery's lib.
